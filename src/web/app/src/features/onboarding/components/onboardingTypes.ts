export interface LanguageItem {
  code: string;
  displayName: string;
  nativeDisplayName: string;
}

export interface OnboardingMember {
  name: string;
  birthDate: string;
  type: string;
  manager: boolean;
}
